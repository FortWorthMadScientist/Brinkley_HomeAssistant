# Copy this file to secrets.yaml in your ESPHome configuration directory.
# NEVER commit your real secrets.yaml to GitHub.

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
fallback_ap_password: "CHANGE_ME_TO_A_STRONG_PASSWORD"

# Optional: only add this if your local ESPHome configuration enables
# encrypted Home Assistant API access.
# api_encryption_key: "YOUR_32_BYTE_BASE64_KEY"
