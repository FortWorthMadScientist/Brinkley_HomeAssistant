# Copy these values into your ESPHome secrets.yaml.
# Never commit your real Wi-Fi password or API encryption key.

wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"

# Optional if you enable ESPHome API encryption:
# api_encryption_key: "YOUR_32_BYTE_BASE64_KEY"
